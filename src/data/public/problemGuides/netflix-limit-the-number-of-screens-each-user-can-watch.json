{
  "prompt": {
    "brief": "Design Design a system to enforce concurrent streaming limits per user account. so users can Track active streaming sessions per account reliably at scale.",
    "successSignals": [
      "Define the source of truth for Track active streaming sessions per account; Enforce screen limits (e.g., 2, 4 screens) and make retries idempotent.",
      "Use bounded, partitioned state to meet Handle millions of concurrent users and Low latency for session validation.",
      "Separate the critical request path from Distributed session store (Redis), Heartbeat mechanism for active sessions, TTL for session expiration.",
      "Explain consistency, failure recovery, authorization, observability, and a degraded mode."
    ]
  },
  "requirements": {
    "functional": [
      "Support the core workflow to Track active streaming sessions per account.",
      "Expose status, results, and freshness appropriate to Design a system to enforce concurrent streaming limits per user account..",
      "Support authorization, validation, updates, deletion, and recovery semantics."
    ],
    "nonFunctional": [
      "Meet Low latency for session validation under normal load.",
      "Scale to Handle millions of concurrent users without a single hot key or unbounded synchronous work.",
      "Do not lose committed state; make retries and duplicate events safe.",
      "Degrade safely when downstream workers, caches, or external dependencies fail."
    ],
    "scaleAssumptions": [
      "Handle millions of concurrent users",
      "Partition by the primary tenant, user, item, or geographic key and isolate hot partitions.",
      "Keep serving state bounded; retain raw events or durable records for replay and auditing."
    ],
    "metrics": [
      {
        "label": "Peak scale",
        "value": "Handle millions of concurrent users",
        "description": "Capacity assumption that drives partitioning and backpressure."
      },
      {
        "label": "Latency target",
        "value": "Low latency for session validation",
        "description": "User-facing budget for the primary request or read path."
      },
      {
        "label": "Durable boundary",
        "value": "Committed before async",
        "description": "The source of truth is Track active streaming sessions per account; Enforce screen limits (e.g., 2, 4 screens)."
      },
      {
        "label": "Async boundary",
        "value": "At-least-once workers",
        "description": "Keep Distributed session store (Redis), Heartbeat mechanism for active sessions, TTL for session expiration off the synchronous path."
      }
    ]
  },
  "entities": [
    {
      "name": "Netflix: Limit the Number of Screens Each User Can Watch",
      "fields": ["id", "owner_id", "state", "created_at", "updated_at"],
      "notes": "Durable model for Track active streaming sessions per account; Enforce screen limits (e.g., 2, 4 screens); design keys around the main reads and writes."
    },
    {
      "name": "netflix-limit-the-number-of-screens-each-user-can-watch event",
      "fields": [
        "eventId",
        "systemToEnforceConcurrentStreamingLimitsPeId",
        "eventType",
        "version",
        "occurredAt",
        "payload"
      ],
      "notes": "Versioned System to enforce concurrent streaming limits pe records preserve ownership, lifecycle, audit, and replay semantics."
    },
    {
      "name": "System to enforce concurrent streaming limits pe policy",
      "fields": [
        "policyId",
        "tenantId",
        "scope",
        "version",
        "rules",
        "updatedAt"
      ],
      "notes": "Versioned System to enforce concurrent streaming limits pe records preserve ownership, lifecycle, audit, and replay semantics."
    },
    {
      "name": "System to enforce concurrent streaming limits pe projection",
      "fields": [
        "systemToEnforceConcurrentStreamingLimitsPeId",
        "status",
        "version",
        "observedAt",
        "freshness",
        "updatedAt"
      ],
      "notes": "Versioned System to enforce concurrent streaming limits pe records preserve ownership, lifecycle, audit, and replay semantics."
    }
  ],
  "apis": [
    {
      "method": "POST",
      "path": "/v1/system-to-enforce-concurrent-streaming-lim",
      "contract": "{ requestId, actorId, input, idempotencyKey } -> { systemToEnforceConcurrentStreamingLimitsPeId, status, version }",
      "notes": "Create or submit System to enforce concurrent streaming limits pe with idempotency and return an accepted state/version."
    },
    {
      "method": "GET",
      "path": "/v1/system-to-enforce-concurrent-streaming-lim/{systemToEnforceConcurrentStreamingLimitsPeId}",
      "contract": "{ systemToEnforceConcurrentStreamingLimitsPeId, status, version, observedAt, freshness }",
      "notes": "Return the latest System to enforce concurrent streaming limits pe state with version, observedAt, and freshness metadata."
    },
    {
      "method": "GET",
      "path": "/v1/system-to-enforce-concurrent-streaming-lim?tenantId=&cursor=&limit=",
      "contract": "{ items[], nextCursor, projectionVersion }",
      "notes": "List tenant-scoped System to enforce concurrent streaming limits pe records with cursor pagination and eligibility filters."
    },
    {
      "method": "POST",
      "path": "/v1/system-to-enforce-concurrent-streaming-lim/rebuild",
      "contract": "{ type, input, idempotencyKey } -> { jobId, status }",
      "notes": "Start a System to enforce concurrent streaming limits pe rebuild or repair operation and return job status."
    }
  ],
  "dataFlow": [
    {
      "title": "Accept Track active streaming sessions per account",
      "description": "The System to enforce concurrent streaming limits pe gateway authenticates the caller, validates the request, applies tenant policy and idempotency, then routes it to the owning service."
    },
    {
      "title": "Commit the durable state",
      "description": "The System to enforce concurrent streaming limits pe service records the accepted state and version before acknowledging the client."
    },
    {
      "title": "Publish the domain event",
      "description": "A durable outbox publishes committed System to enforce concurrent streaming limits pe events; consumers deduplicate by event ID and track offsets."
    },
    {
      "title": "Build the read model",
      "description": "Partitioned workers consume System to enforce concurrent streaming limits pe events, update the serving projection, and expose lag or watermark metadata."
    },
    {
      "title": "Serve and recover asynchronously",
      "description": "Serving reads use the System to enforce concurrent streaming limits pe projection; replay, repair, export, and slow work remain off the request path."
    }
  ],
  "architecture": {
    "title": "Design a system to enforce concurrent streaming limits per user account. reference architecture",
    "summary": "The authoritative state records Track active streaming sessions per account; asynchronous workers build serving projections while Distributed session store (Redis), Heartbeat mechanism for active sessions, TTL for session expiration stays isolated from the request path.",
    "layers": [
      {
        "id": "experience",
        "label": "System to enforce concurrent streaming limits pe request boundary",
        "description": "Clients submit and read System to enforce concurrent streaming limits pe requests through an authenticated boundary.",
        "componentIds": ["client", "gateway", "service"]
      },
      {
        "id": "source",
        "label": "System to enforce concurrent streaming limits pe authoritative state",
        "description": "Durable System to enforce concurrent streaming limits pe state supports replay, repair, and consistency checks.",
        "componentIds": ["source", "stream"]
      },
      {
        "id": "processing",
        "label": "System to enforce concurrent streaming limits pe processing and serving",
        "description": "Partitioned System to enforce concurrent streaming limits pe workers build serving projections with visible freshness.",
        "componentIds": ["workers", "read-model"]
      },
      {
        "id": "operations",
        "label": "System to enforce concurrent streaming limits pe operations",
        "description": "System to enforce concurrent streaming limits pe signals expose lag, failures, repair, correctness, and capacity.",
        "componentIds": ["monitoring"]
      }
    ],
    "keyPaths": [
      {
        "id": "critical",
        "label": "System to enforce concurrent streaming limits pe write path",
        "description": "Client -> System to enforce concurrent streaming limits pe gateway -> service -> authoritative state.",
        "componentIds": ["client", "gateway", "service", "source"]
      },
      {
        "id": "async",
        "label": "System to enforce concurrent streaming limits pe projection path",
        "description": "Authoritative System to enforce concurrent streaming limits pe state -> event backbone -> workers -> serving projection.",
        "componentIds": ["source", "stream", "workers", "read-model"]
      }
    ],
    "components": [
      {
        "id": "client",
        "type": "client",
        "componentId": "web-app",
        "label": "System to enforce concurrent streaming limits pe clients",
        "description": "Clients use the System to enforce concurrent streaming limits pe workflow and receive explicit status, freshness, or progress.",
        "position": {
          "x": 0,
          "y": 260
        },
        "properties": {
          "protocol": "HTTPS or streaming API"
        }
      },
      {
        "id": "gateway",
        "type": "api-gateway",
        "componentId": "api-gateway",
        "label": "System to enforce concurrent streaming limits pe gateway",
        "description": "Authenticates System to enforce concurrent streaming limits pe requests, enforces tenant isolation, quotas, and idempotency, and routes by resource key.",
        "position": {
          "x": 360,
          "y": 260
        },
        "properties": {
          "protection": "Auth, validation, rate limits"
        }
      },
      {
        "id": "service",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "System to enforce concurrent streaming limits pe service",
        "description": "Owns System to enforce concurrent streaming limits pe commands, authorization, lifecycle state, and the client contract.",
        "position": {
          "x": 720,
          "y": 260
        },
        "properties": {
          "consistency": "Idempotent command handling"
        }
      },
      {
        "id": "source",
        "type": "database",
        "componentId": "database",
        "label": "System to enforce concurrent streaming limits pe state",
        "description": "Stores authoritative System to enforce concurrent streaming limits pe state and supports the primary access patterns.",
        "position": {
          "x": 1080,
          "y": 260
        },
        "properties": {
          "storage": "Netflix: Limit the Number of Screens Each User Can Watch"
        }
      },
      {
        "id": "stream",
        "type": "message-broker",
        "componentId": "message-broker",
        "label": "System to enforce concurrent streaming limits pe event backbone",
        "description": "Publishes committed System to enforce concurrent streaming limits pe events with stable keys so consumers can retry and replay safely.",
        "position": {
          "x": 1440,
          "y": 100
        },
        "properties": {
          "delivery": "At least once with dedupe"
        }
      },
      {
        "id": "workers",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "System to enforce concurrent streaming limits pe workers",
        "description": "Workers process System to enforce concurrent streaming limits pe events asynchronously, retry transient failures, and update projections.",
        "position": {
          "x": 1800,
          "y": 100
        },
        "properties": {
          "scaling": "Partitioned workers"
        }
      },
      {
        "id": "read-model",
        "type": "cache",
        "componentId": "cache",
        "label": "System to enforce concurrent streaming limits pe projection",
        "description": "Serves low-latency System to enforce concurrent streaming limits pe views with explicit freshness and fallback behavior.",
        "position": {
          "x": 2160,
          "y": 260
        },
        "properties": {
          "latency": "Low latency for session validation"
        }
      },
      {
        "id": "monitoring",
        "type": "monitoring",
        "componentId": "monitoring",
        "label": "System to enforce concurrent streaming limits pe monitoring",
        "description": "Tracks System to enforce concurrent streaming limits pe latency, backlog, freshness, correctness, and saturation.",
        "position": {
          "x": 1800,
          "y": -180
        },
        "properties": {
          "alerts": "Errors, lag, drift"
        }
      }
    ],
    "connections": [
      {
        "id": "client-gateway",
        "source": "client",
        "target": "gateway",
        "type": "http",
        "label": "Start System to enforce concurrent streaming limits pe",
        "description": "The client submits an authenticated System to enforce concurrent streaming limits pe request through the edge boundary."
      },
      {
        "id": "gateway-service",
        "source": "gateway",
        "target": "service",
        "type": "api-call",
        "label": "Validated System to enforce concurrent streaming limits pe operation",
        "description": "The gateway forwards a validated System to enforce concurrent streaming limits pe operation after authentication, quota, and idempotency checks."
      },
      {
        "id": "service-source",
        "source": "service",
        "target": "source",
        "type": "database-connection",
        "label": "System to enforce concurrent streaming limits pe state commit",
        "description": "The System to enforce concurrent streaming limits pe service commits authoritative state before downstream asynchronous work begins."
      },
      {
        "id": "source-stream",
        "source": "source",
        "target": "stream",
        "type": "event-stream",
        "label": "System to enforce concurrent streaming limits pe event",
        "description": "An outbox or change stream publishes committed System to enforce concurrent streaming limits pe transitions with replayable ordering."
      },
      {
        "id": "stream-workers",
        "source": "stream",
        "target": "workers",
        "type": "message-queue",
        "label": "System to enforce concurrent streaming limits pe background work",
        "description": "Workers consume committed System to enforce concurrent streaming limits pe events, perform asynchronous work, and update serving state."
      },
      {
        "id": "workers-read-model",
        "source": "workers",
        "target": "read-model",
        "type": "data-flow",
        "label": "System to enforce concurrent streaming limits pe serving update",
        "description": "Workers update the System to enforce concurrent streaming limits pe projection and expose a freshness watermark."
      },
      {
        "id": "read-service",
        "source": "read-model",
        "target": "service",
        "type": "database-connection",
        "label": "System to enforce concurrent streaming limits pe projection read",
        "description": "The System to enforce concurrent streaming limits pe service reads the latest acceptable projection or authoritative state."
      },
      {
        "id": "service-client",
        "source": "service",
        "target": "client",
        "type": "http",
        "label": "System to enforce concurrent streaming limits pe result",
        "description": "The System to enforce concurrent streaming limits pe read path returns the latest acceptable result within its latency target."
      },
      {
        "id": "stream-monitoring",
        "source": "stream",
        "target": "monitoring",
        "type": "data-flow",
        "label": "System to enforce concurrent streaming limits pe pipeline health",
        "description": "Monitors System to enforce concurrent streaming limits pe consumer lag, processing health, freshness, and projection correctness."
      }
    ]
  },
  "deepDives": [
    {
      "title": "Consistency and idempotency",
      "points": [
        "Keep the System to enforce concurrent streaming limits pe commit authoritative; let derived projections converge with visible version and freshness.",
        "Use idempotency keys, conditional System to enforce concurrent streaming limits pe writes, event IDs, and versions so retries and replay are safe.",
        "Return System to enforce concurrent streaming limits pe version and observedAt so clients can reason about freshness."
      ]
    },
    {
      "title": "Partitioning and hot keys",
      "points": [
        "Partition System to enforce concurrent streaming limits pe by its primary read and write keys, then isolate hot tenants, users, items, or regions.",
        "Bound System to enforce concurrent streaming limits pe work with backpressure and queue isolation so one hot key cannot dominate the cluster.",
        "Keep a replay or reconciliation path for System to enforce concurrent streaming limits pe projection drift and partial failures."
      ]
    },
    {
      "title": "Failure and recovery",
      "points": [
        "Serve the last acceptable System to enforce concurrent streaming limits pe projection or an explicit degraded response when workers fail.",
        "Retain durable System to enforce concurrent streaming limits pe records for replay and make each downstream effect independently retryable.",
        "Measure System to enforce concurrent streaming limits pe latency, backlog, freshness, error rate, and correctness drift."
      ]
    }
  ],
  "tradeoffs": [
    {
      "title": "Strong consistency versus throughput",
      "recommendation": "Strongly protect the business commit and use versioned eventual projections for high-volume reads.",
      "caution": "Making every derived read synchronous increases coordination and tail latency."
    },
    {
      "title": "Push versus pull processing",
      "recommendation": "Push bounded events to workers and pull durable snapshots on reads or recovery.",
      "caution": "Unbounded fanout or full scans make bursts and hot keys expensive."
    },
    {
      "title": "Managed service versus custom control",
      "recommendation": "Start with managed primitives where their limits match the access pattern, then isolate replaceable interfaces.",
      "caution": "Provider defaults do not remove the need for quotas, replay, observability, and data lifecycle design."
    }
  ],
  "commonMistakes": [
    "Putting all work on the synchronous request path.",
    "Choosing a data store before writing the access patterns and consistency contract.",
    "Ignoring retries, duplicate events, late data, deletion, privacy, or authorization.",
    "Leaving hot partitions, backpressure, stale reads, and recovery unspecified.",
    "Allowing unbounded requests or exports to compete with normal traffic."
  ],
  "followUps": [
    {
      "question": "What happens when the primary worker is unavailable?",
      "answer": "Acknowledge only after the durable boundary, serve the last acceptable projection, queue retryable work, and replay or reconcile after recovery."
    },
    {
      "question": "How would you support a tenfold traffic spike?",
      "answer": "Pre-scale partitions, apply admission control and backpressure, isolate hot keys, cache immutable snapshots, and make freshness degradation visible."
    },
    {
      "question": "How do you verify correctness?",
      "answer": "Use idempotency metrics, version gaps, sampled source-to-projection comparisons, replay tests, and a shadow rebuild before repair."
    }
  ],
  "rubric": [
    {
      "criterion": "Requirements",
      "description": "The answer defines the Track active streaming sessions per account workflow, scale, latency, durability, and failure contract."
    },
    {
      "criterion": "Data model",
      "description": "Entities and keys support Design a system to enforce concurrent streaming limits per user account., authorization, privacy, and lifecycle changes."
    },
    {
      "criterion": "Scalability",
      "description": "Partitioning, cache behavior, queues, backpressure, and hot-key handling are explicit."
    },
    {
      "criterion": "Reliability",
      "description": "Idempotency, replay, recovery, degraded reads, and observability protect committed state."
    },
    {
      "criterion": "Tradeoffs",
      "description": "The candidate can explain System to enforce concurrent streaming limits pe consistency, push/pull, managed services, and the scale boundary."
    }
  ]
}
