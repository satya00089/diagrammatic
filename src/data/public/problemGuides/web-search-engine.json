{
  "prompt": {
    "brief": "Design crawl, index, retrieve, rank, and serve web search results so users can answer a search query reliably at scale.",
    "successSignals": [
      "Define the source of truth for document index and crawl metadata and make retries idempotent.",
      "Use bounded, partitioned state to meet billions of documents and 100K queries per second and p95 <=200ms search results.",
      "Separate the critical request path from crawling, parsing, indexing, ranking, and refresh.",
      "Explain consistency, failure recovery, authorization, observability, and a degraded mode."
    ]
  },
  "requirements": {
    "functional": [
      "Support the core workflow to answer a search query.",
      "Expose status, results, and freshness appropriate to crawl, index, retrieve, rank, and serve web search results.",
      "Support authorization, validation, updates, deletion, and recovery semantics."
    ],
    "nonFunctional": [
      "Meet p95 <=200ms search results under normal load.",
      "Scale to billions of documents and 100K queries per second without a single hot key or unbounded synchronous work.",
      "Do not lose committed state; make retries and duplicate events safe.",
      "Degrade safely when downstream workers, caches, or external dependencies fail."
    ],
    "scaleAssumptions": [
      "billions of documents and 100K queries per second",
      "Partition by the primary tenant, user, item, or geographic key and isolate hot partitions.",
      "Keep serving state bounded; retain raw events or durable records for replay and auditing."
    ],
    "metrics": [
      {
        "label": "Peak scale",
        "value": "billions of documents",
        "description": "Capacity assumption that drives partitioning and backpressure."
      },
      {
        "label": "Latency target",
        "value": "p95 <=200ms search results",
        "description": "User-facing budget for the primary request or read path."
      },
      {
        "label": "Durable boundary",
        "value": "Committed before async",
        "description": "The source of truth is document index and crawl metadata."
      },
      {
        "label": "Async boundary",
        "value": "At-least-once workers",
        "description": "Keep crawling, parsing, indexing, ranking, and refresh off the synchronous path."
      }
    ]
  },
  "entities": [
    {
      "name": "inverted index and document store",
      "fields": ["id", "owner_id", "state", "created_at", "updated_at"],
      "notes": "Durable model for document index and crawl metadata; design keys around the main reads and writes."
    },
    {
      "name": "web-search event",
      "fields": [
        "eventId",
        "crawlIndexRetrieveRankId",
        "eventType",
        "version",
        "occurredAt",
        "payload"
      ],
      "notes": "Versioned, idempotent state supports retries, replay, and consistency checks."
    },
    {
      "name": "Policy and configuration",
      "fields": [
        "policyId",
        "tenantId",
        "scope",
        "version",
        "rules",
        "updatedAt"
      ],
      "notes": "Versioned, idempotent state supports retries, replay, and consistency checks."
    },
    {
      "name": "Read model",
      "fields": [
        "crawlIndexRetrieveRankId",
        "status",
        "version",
        "observedAt",
        "freshness",
        "updatedAt"
      ],
      "notes": "Versioned, idempotent state supports retries, replay, and consistency checks."
    }
  ],
  "apis": [
    {
      "method": "POST",
      "path": "/v1/crawl-index-retrieve-rank",
      "contract": "{ requestId, actorId, input, idempotencyKey } -> { crawlIndexRetrieveRankId, status, version }",
      "notes": "Create or submit Crawl, index, retrieve, rank, with idempotency and return an accepted state/version."
    },
    {
      "method": "GET",
      "path": "/v1/crawl-index-retrieve-rank/{crawlIndexRetrieveRankId}",
      "contract": "{ crawlIndexRetrieveRankId, status, version, observedAt, freshness }",
      "notes": "Return the latest Crawl, index, retrieve, rank, state with version, observedAt, and freshness metadata."
    },
    {
      "method": "GET",
      "path": "/v1/crawl-index-retrieve-rank?tenantId=&cursor=&limit=",
      "contract": "{ items[], nextCursor, projectionVersion }",
      "notes": "List tenant-scoped Crawl, index, retrieve, rank, records with cursor pagination and eligibility filters."
    },
    {
      "method": "POST",
      "path": "/v1/crawl-index-retrieve-rank/rebuild",
      "contract": "{ type, input, idempotencyKey } -> { jobId, status }",
      "notes": "Start a Crawl, index, retrieve, rank, rebuild or repair operation and return job status."
    }
  ],
  "dataFlow": [
    {
      "title": "Accept answer a search query",
      "description": "The Crawl, index, retrieve, rank, gateway authenticates the caller, validates the request, applies tenant policy and idempotency, then routes it to the owning service."
    },
    {
      "title": "Commit the durable state",
      "description": "The Crawl, index, retrieve, rank, service records the accepted state and version before acknowledging the client."
    },
    {
      "title": "Publish the domain event",
      "description": "A durable outbox publishes committed Crawl, index, retrieve, rank, events; consumers deduplicate by event ID and track offsets."
    },
    {
      "title": "Build the read model",
      "description": "Partitioned workers consume Crawl, index, retrieve, rank, events, update the serving projection, and expose lag or watermark metadata."
    },
    {
      "title": "Serve and recover asynchronously",
      "description": "Serving reads use the Crawl, index, retrieve, rank, projection; replay, repair, export, and slow work remain off the request path."
    }
  ],
  "architecture": {
    "title": "Crawl, index, retrieve, rank, and serve web search results reference architecture",
    "summary": "The authoritative state records answer a search query; asynchronous workers build serving projections while crawling, parsing, indexing, ranking, and refresh stays isolated from the request path.",
    "layers": [
      {
        "id": "experience",
        "label": "Crawl, index, retrieve, rank, request boundary",
        "description": "Clients submit and read Crawl, index, retrieve, rank, requests through an authenticated boundary.",
        "componentIds": ["client", "gateway", "service"]
      },
      {
        "id": "source",
        "label": "Crawl, index, retrieve, rank, authoritative state",
        "description": "Durable Crawl, index, retrieve, rank, state supports replay, repair, and consistency checks.",
        "componentIds": ["source", "stream"]
      },
      {
        "id": "processing",
        "label": "Crawl, index, retrieve, rank, processing and serving",
        "description": "Partitioned Crawl, index, retrieve, rank, workers build serving projections with visible freshness.",
        "componentIds": ["workers", "read-model"]
      },
      {
        "id": "operations",
        "label": "Crawl, index, retrieve, rank, operations",
        "description": "Crawl, index, retrieve, rank, signals expose lag, failures, repair, correctness, and capacity.",
        "componentIds": ["monitoring"]
      }
    ],
    "keyPaths": [
      {
        "id": "critical",
        "label": "Crawl, index, retrieve, rank, write path",
        "description": "Client -> Crawl, index, retrieve, rank, gateway -> service -> authoritative state.",
        "componentIds": ["client", "gateway", "service", "source"]
      },
      {
        "id": "async",
        "label": "Crawl, index, retrieve, rank, projection path",
        "description": "Authoritative Crawl, index, retrieve, rank, state -> event backbone -> workers -> serving projection.",
        "componentIds": ["source", "stream", "workers", "read-model"]
      }
    ],
    "components": [
      {
        "id": "client",
        "type": "client",
        "componentId": "web-app",
        "label": "Design crawl, index, retrieve, rank, clients",
        "description": "Clients use the answer a search query workflow and receive explicit status, freshness, or progress.",
        "position": {
          "x": 0,
          "y": 260
        },
        "properties": {
          "protocol": "HTTPS or streaming API"
        }
      },
      {
        "id": "gateway",
        "type": "api-gateway",
        "componentId": "api-gateway",
        "label": "Design crawl, index, retrieve, rank, gateway",
        "description": "Authenticates Design crawl, index, retrieve, rank, requests, enforces tenant isolation, quotas, and idempotency, and routes by resource key.",
        "position": {
          "x": 360,
          "y": 260
        },
        "properties": {
          "protection": "Auth, validation, rate limits"
        }
      },
      {
        "id": "service",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "Design crawl, index, retrieve, rank, service",
        "description": "Owns the answer a search query command, authorization policy, lifecycle state, and client contract.",
        "position": {
          "x": 720,
          "y": 260
        },
        "properties": {
          "consistency": "Idempotent command handling"
        }
      },
      {
        "id": "source",
        "type": "database",
        "componentId": "database",
        "label": "Design crawl, index, retrieve, rank, state",
        "description": "Stores document index and crawl metadata and supports the primary access patterns.",
        "position": {
          "x": 1080,
          "y": 260
        },
        "properties": {
          "storage": "inverted index and document store"
        }
      },
      {
        "id": "stream",
        "type": "message-broker",
        "componentId": "message-broker",
        "label": "Design crawl, index, retrieve, rank, event backbone",
        "description": "Publishes versioned events for crawling, parsing, indexing, ranking, and refresh after durable commits.",
        "position": {
          "x": 1440,
          "y": 100
        },
        "properties": {
          "delivery": "At least once with dedupe"
        }
      },
      {
        "id": "workers",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "Design crawl, index, retrieve, rank, workers",
        "description": "Workers process crawling, parsing, indexing, ranking, and refresh independently from the user-facing command and retry transient failures.",
        "position": {
          "x": 1800,
          "y": 100
        },
        "properties": {
          "scaling": "Partitioned workers"
        }
      },
      {
        "id": "read-model",
        "type": "cache",
        "componentId": "cache",
        "label": "Design crawl, index, retrieve, rank, projection",
        "description": "Serves low-latency views for inverted index and document store with explicit freshness and fallback behavior.",
        "position": {
          "x": 2160,
          "y": 260
        },
        "properties": {
          "latency": "p95 <=200ms search results"
        }
      },
      {
        "id": "monitoring",
        "type": "monitoring",
        "componentId": "monitoring",
        "label": "Design crawl, index, retrieve, rank, monitoring",
        "description": "Tracks Design crawl, index, retrieve, rank, latency, backlog, freshness, correctness, and saturation.",
        "position": {
          "x": 1800,
          "y": -180
        },
        "properties": {
          "alerts": "Errors, lag, drift"
        }
      }
    ],
    "connections": [
      {
        "id": "client-gateway",
        "source": "client",
        "target": "gateway",
        "type": "http",
        "label": "Start Design crawl, index, retrieve, rank,",
        "description": "Client submits answer a search query through the authenticated request boundary."
      },
      {
        "id": "gateway-service",
        "source": "gateway",
        "target": "service",
        "type": "api-call",
        "label": "Validated Design crawl, index, retrieve, rank, operation",
        "description": "The gateway forwards a validated Design crawl, index, retrieve, rank, operation after authentication, quota, and idempotency checks."
      },
      {
        "id": "service-source",
        "source": "service",
        "target": "source",
        "type": "database-connection",
        "label": "Design crawl, index, retrieve, rank, state commit",
        "description": "Commits document index and crawl metadata before downstream asynchronous work."
      },
      {
        "id": "source-stream",
        "source": "source",
        "target": "stream",
        "type": "event-stream",
        "label": "Design crawl, index, retrieve, rank, event",
        "description": "An outbox or change stream publishes committed Design crawl, index, retrieve, rank, transitions with replayable ordering."
      },
      {
        "id": "stream-workers",
        "source": "stream",
        "target": "workers",
        "type": "message-queue",
        "label": "Design crawl, index, retrieve, rank, background work",
        "description": "Workers handle crawling, parsing, indexing, ranking, and refresh with bounded retries and idempotent updates."
      },
      {
        "id": "workers-read-model",
        "source": "workers",
        "target": "read-model",
        "type": "data-flow",
        "label": "Design crawl, index, retrieve, rank, serving update",
        "description": "Workers update the Design crawl, index, retrieve, rank, projection and expose a freshness watermark."
      },
      {
        "id": "read-service",
        "source": "read-model",
        "target": "service",
        "type": "database-connection",
        "label": "Design crawl, index, retrieve, rank, projection read",
        "description": "The Design crawl, index, retrieve, rank, service reads the latest acceptable projection or authoritative state."
      },
      {
        "id": "service-client",
        "source": "service",
        "target": "client",
        "type": "http",
        "label": "Design crawl, index, retrieve, rank, result",
        "description": "Returns the latest acceptable result within p95 <=200ms search results."
      },
      {
        "id": "stream-monitoring",
        "source": "stream",
        "target": "monitoring",
        "type": "data-flow",
        "label": "Design crawl, index, retrieve, rank, pipeline health",
        "description": "Monitors Design crawl, index, retrieve, rank, consumer lag, processing health, freshness, and projection correctness."
      }
    ]
  },
  "deepDives": [
    {
      "title": "Consistency and idempotency",
      "points": [
        "Keep the Crawl, index, retrieve, rank, commit authoritative; let derived projections converge with visible version and freshness.",
        "Use idempotency keys, conditional Crawl, index, retrieve, rank, writes, event IDs, and versions so retries and replay are safe.",
        "Return Crawl, index, retrieve, rank, version and observedAt so clients can reason about freshness."
      ]
    },
    {
      "title": "Partitioning and hot keys",
      "points": [
        "Partition Crawl, index, retrieve, rank, by its primary read and write keys, then isolate hot tenants, users, items, or regions.",
        "Bound Crawl, index, retrieve, rank, work with backpressure and queue isolation so one hot key cannot dominate the cluster.",
        "Keep a replay or reconciliation path for Crawl, index, retrieve, rank, projection drift and partial failures."
      ]
    },
    {
      "title": "Failure and recovery",
      "points": [
        "Serve the last acceptable Crawl, index, retrieve, rank, projection or an explicit degraded response when workers fail.",
        "Retain durable Crawl, index, retrieve, rank, records for replay and make each downstream effect independently retryable.",
        "Measure Crawl, index, retrieve, rank, latency, backlog, freshness, error rate, and correctness drift."
      ]
    }
  ],
  "tradeoffs": [
    {
      "title": "Strong consistency versus throughput",
      "recommendation": "Strongly protect the business commit and use versioned eventual projections for high-volume reads.",
      "caution": "Making every derived read synchronous increases coordination and tail latency."
    },
    {
      "title": "Push versus pull processing",
      "recommendation": "Push bounded events to workers and pull durable snapshots on reads or recovery.",
      "caution": "Unbounded fanout or full scans make bursts and hot keys expensive."
    },
    {
      "title": "Managed service versus custom control",
      "recommendation": "Start with managed primitives where their limits match the access pattern, then isolate replaceable interfaces.",
      "caution": "Provider defaults do not remove the need for quotas, replay, observability, and data lifecycle design."
    }
  ],
  "commonMistakes": [
    "Putting all work on the synchronous request path.",
    "Choosing a data store before writing the access patterns and consistency contract.",
    "Ignoring retries, duplicate events, late data, deletion, privacy, or authorization.",
    "Leaving hot partitions, backpressure, stale reads, and recovery unspecified.",
    "Allowing unbounded requests or exports to compete with normal traffic."
  ],
  "followUps": [
    {
      "question": "What happens when the primary worker is unavailable?",
      "answer": "Acknowledge only after the durable boundary, serve the last acceptable projection, queue retryable work, and replay or reconcile after recovery."
    },
    {
      "question": "How would you support a tenfold traffic spike?",
      "answer": "Pre-scale partitions, apply admission control and backpressure, isolate hot keys, cache immutable snapshots, and make freshness degradation visible."
    },
    {
      "question": "How do you verify correctness?",
      "answer": "Use idempotency metrics, version gaps, sampled source-to-projection comparisons, replay tests, and a shadow rebuild before repair."
    }
  ],
  "rubric": [
    {
      "criterion": "Requirements",
      "description": "The answer defines the answer a search query workflow, scale, latency, durability, and failure contract."
    },
    {
      "criterion": "Data model",
      "description": "Entities and keys support crawl, index, retrieve, rank, and serve web search results, authorization, privacy, and lifecycle changes."
    },
    {
      "criterion": "Scalability",
      "description": "Partitioning, cache behavior, queues, backpressure, and hot-key handling are explicit."
    },
    {
      "criterion": "Reliability",
      "description": "Idempotency, replay, recovery, degraded reads, and observability protect committed state."
    },
    {
      "criterion": "Tradeoffs",
      "description": "The candidate can explain Crawl, index, retrieve, rank, consistency, push/pull, managed services, and the scale boundary."
    }
  ]
}
