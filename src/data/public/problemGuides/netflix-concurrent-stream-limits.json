{
  "prompt": {
    "brief": "Design enforce per-account concurrent playback limits across devices and regions so users can start or renew a playback session reliably at scale.",
    "successSignals": [
      "Define the source of truth for session intent and device policy and make retries idempotent.",
      "Use bounded, partitioned state to meet 100M accounts and millions of session heartbeats and playback authorization p95 <=100ms.",
      "Separate the critical request path from heartbeat expiry, analytics, and entitlement refresh.",
      "Explain consistency, failure recovery, authorization, observability, and a degraded mode."
    ]
  },
  "requirements": {
    "functional": [
      "Support the core workflow to start or renew a playback session.",
      "Expose status, results, and freshness appropriate to enforce per-account concurrent playback limits across devices and regions.",
      "Support authorization, validation, updates, deletion, and recovery semantics."
    ],
    "nonFunctional": [
      "Meet playback authorization p95 <=100ms under normal load.",
      "Scale to 100M accounts and millions of session heartbeats without a single hot key or unbounded synchronous work.",
      "Do not lose committed state; make retries and duplicate events safe.",
      "Degrade safely when downstream workers, caches, or external dependencies fail."
    ],
    "scaleAssumptions": [
      "100M accounts and millions of session heartbeats",
      "Partition by the primary tenant, user, item, or geographic key and isolate hot partitions.",
      "Keep serving state bounded; retain raw events or durable records for replay and auditing."
    ],
    "metrics": [
      {
        "label": "Peak scale",
        "value": "100M accounts",
        "description": "Capacity assumption that drives partitioning and backpressure."
      },
      {
        "label": "Latency target",
        "value": "playback authorization p95 <=100ms",
        "description": "User-facing budget for the primary request or read path."
      },
      {
        "label": "Durable boundary",
        "value": "Committed before async",
        "description": "The source of truth is session intent and device policy."
      },
      {
        "label": "Async boundary",
        "value": "At-least-once workers",
        "description": "Keep heartbeat expiry, analytics, and entitlement refresh off the synchronous path."
      }
    ]
  },
  "entities": [
    {
      "name": "account limits and active leases",
      "fields": ["id", "owner_id", "state", "created_at", "updated_at"],
      "notes": "Durable model for session intent and device policy; design keys around the main reads and writes."
    },
    {
      "name": "netflix-concurrent-stream-limits event",
      "fields": [
        "eventId",
        "enforcePerAccountConcurrentPlaybackLimitsId",
        "eventType",
        "version",
        "occurredAt",
        "payload"
      ],
      "notes": "Versioned Enforce per-account concurrent playback limits records preserve ownership, lifecycle, audit, and replay semantics."
    },
    {
      "name": "Enforce per-account concurrent playback limits policy",
      "fields": [
        "policyId",
        "tenantId",
        "scope",
        "version",
        "rules",
        "updatedAt"
      ],
      "notes": "Versioned Enforce per-account concurrent playback limits records preserve ownership, lifecycle, audit, and replay semantics."
    },
    {
      "name": "Enforce per-account concurrent playback limits projection",
      "fields": [
        "enforcePerAccountConcurrentPlaybackLimitsId",
        "status",
        "version",
        "observedAt",
        "freshness",
        "updatedAt"
      ],
      "notes": "Versioned Enforce per-account concurrent playback limits records preserve ownership, lifecycle, audit, and replay semantics."
    }
  ],
  "apis": [
    {
      "method": "POST",
      "path": "/v1/enforce-per-account-concurrent-playback-li",
      "contract": "{ requestId, actorId, input, idempotencyKey } -> { enforcePerAccountConcurrentPlaybackLimitsId, status, version }",
      "notes": "Create or submit Enforce per-account concurrent playback limits with idempotency and return an accepted state/version."
    },
    {
      "method": "GET",
      "path": "/v1/enforce-per-account-concurrent-playback-li/{enforcePerAccountConcurrentPlaybackLimitsId}",
      "contract": "{ enforcePerAccountConcurrentPlaybackLimitsId, status, version, observedAt, freshness }",
      "notes": "Return the latest Enforce per-account concurrent playback limits state with version, observedAt, and freshness metadata."
    },
    {
      "method": "GET",
      "path": "/v1/enforce-per-account-concurrent-playback-li?tenantId=&cursor=&limit=",
      "contract": "{ items[], nextCursor, projectionVersion }",
      "notes": "List tenant-scoped Enforce per-account concurrent playback limits records with cursor pagination and eligibility filters."
    },
    {
      "method": "POST",
      "path": "/v1/enforce-per-account-concurrent-playback-li/rebuild",
      "contract": "{ type, input, idempotencyKey } -> { jobId, status }",
      "notes": "Start a Enforce per-account concurrent playback limits rebuild or repair operation and return job status."
    }
  ],
  "dataFlow": [
    {
      "title": "Accept start or renew a playback session",
      "description": "The Enforce per-account concurrent playback limits gateway authenticates the caller, validates the request, applies tenant policy and idempotency, then routes it to the owning service."
    },
    {
      "title": "Commit the durable state",
      "description": "The Enforce per-account concurrent playback limits service records the accepted state and version before acknowledging the client."
    },
    {
      "title": "Publish the domain event",
      "description": "A durable outbox publishes committed Enforce per-account concurrent playback limits events; consumers deduplicate by event ID and track offsets."
    },
    {
      "title": "Build the read model",
      "description": "Partitioned workers consume Enforce per-account concurrent playback limits events, update the serving projection, and expose lag or watermark metadata."
    },
    {
      "title": "Serve and recover asynchronously",
      "description": "Serving reads use the Enforce per-account concurrent playback limits projection; replay, repair, export, and slow work remain off the request path."
    }
  ],
  "architecture": {
    "title": "Enforce per-account concurrent playback limits across devices and regions reference architecture",
    "summary": "The authoritative state records start or renew a playback session; asynchronous workers build serving projections while heartbeat expiry, analytics, and entitlement refresh stays isolated from the request path.",
    "layers": [
      {
        "id": "experience",
        "label": "Enforce per-account concurrent playback limits request boundary",
        "description": "Clients submit and read Enforce per-account concurrent playback limits requests through an authenticated boundary.",
        "componentIds": ["client", "gateway", "service"]
      },
      {
        "id": "source",
        "label": "Enforce per-account concurrent playback limits authoritative state",
        "description": "Durable Enforce per-account concurrent playback limits state supports replay, repair, and consistency checks.",
        "componentIds": ["source", "stream"]
      },
      {
        "id": "processing",
        "label": "Enforce per-account concurrent playback limits processing and serving",
        "description": "Partitioned Enforce per-account concurrent playback limits workers build serving projections with visible freshness.",
        "componentIds": ["workers", "read-model"]
      },
      {
        "id": "operations",
        "label": "Enforce per-account concurrent playback limits operations",
        "description": "Enforce per-account concurrent playback limits signals expose lag, failures, repair, correctness, and capacity.",
        "componentIds": ["monitoring"]
      }
    ],
    "keyPaths": [
      {
        "id": "critical",
        "label": "Enforce per-account concurrent playback limits write path",
        "description": "Client -> Enforce per-account concurrent playback limits gateway -> service -> authoritative state.",
        "componentIds": ["client", "gateway", "service", "source"]
      },
      {
        "id": "async",
        "label": "Enforce per-account concurrent playback limits projection path",
        "description": "Authoritative Enforce per-account concurrent playback limits state -> event backbone -> workers -> serving projection.",
        "componentIds": ["source", "stream", "workers", "read-model"]
      }
    ],
    "components": [
      {
        "id": "client",
        "type": "client",
        "componentId": "web-app",
        "label": "Design enforce per-account concurrent playback l clients",
        "description": "Clients use the start or renew a playback session workflow and receive explicit status, freshness, or progress.",
        "position": {
          "x": 0,
          "y": 260
        },
        "properties": {
          "protocol": "HTTPS or streaming API"
        }
      },
      {
        "id": "gateway",
        "type": "api-gateway",
        "componentId": "api-gateway",
        "label": "Design enforce per-account concurrent playback l gateway",
        "description": "Authenticates Design enforce per-account concurrent playback l requests, enforces tenant isolation, quotas, and idempotency, and routes by resource key.",
        "position": {
          "x": 360,
          "y": 260
        },
        "properties": {
          "protection": "Auth, validation, rate limits"
        }
      },
      {
        "id": "service",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "Design enforce per-account concurrent playback l service",
        "description": "Owns the start or renew a playback session command, authorization policy, lifecycle state, and client contract.",
        "position": {
          "x": 720,
          "y": 260
        },
        "properties": {
          "consistency": "Idempotent command handling"
        }
      },
      {
        "id": "source",
        "type": "database",
        "componentId": "database",
        "label": "Design enforce per-account concurrent playback l state",
        "description": "Stores session intent and device policy and supports the primary access patterns.",
        "position": {
          "x": 1080,
          "y": 260
        },
        "properties": {
          "storage": "account limits and active leases"
        }
      },
      {
        "id": "stream",
        "type": "message-broker",
        "componentId": "message-broker",
        "label": "Design enforce per-account concurrent playback l event backbone",
        "description": "Publishes versioned events for heartbeat expiry, analytics, and entitlement refresh after durable commits.",
        "position": {
          "x": 1440,
          "y": 100
        },
        "properties": {
          "delivery": "At least once with dedupe"
        }
      },
      {
        "id": "workers",
        "type": "microservice",
        "componentId": "backend-server",
        "label": "Design enforce per-account concurrent playback l workers",
        "description": "Workers process heartbeat expiry, analytics, and entitlement refresh independently from the user-facing command and retry transient failures.",
        "position": {
          "x": 1800,
          "y": 100
        },
        "properties": {
          "scaling": "Partitioned workers"
        }
      },
      {
        "id": "read-model",
        "type": "cache",
        "componentId": "cache",
        "label": "Design enforce per-account concurrent playback l projection",
        "description": "Serves low-latency views for account limits and active leases with explicit freshness and fallback behavior.",
        "position": {
          "x": 2160,
          "y": 260
        },
        "properties": {
          "latency": "playback authorization p95 <=100ms"
        }
      },
      {
        "id": "monitoring",
        "type": "monitoring",
        "componentId": "monitoring",
        "label": "Design enforce per-account concurrent playback l monitoring",
        "description": "Tracks Design enforce per-account concurrent playback l latency, backlog, freshness, correctness, and saturation.",
        "position": {
          "x": 1800,
          "y": -180
        },
        "properties": {
          "alerts": "Errors, lag, drift"
        }
      }
    ],
    "connections": [
      {
        "id": "client-gateway",
        "source": "client",
        "target": "gateway",
        "type": "http",
        "label": "Start Design enforce per-account concurrent playback l",
        "description": "Client submits start or renew a playback session through the authenticated request boundary."
      },
      {
        "id": "gateway-service",
        "source": "gateway",
        "target": "service",
        "type": "api-call",
        "label": "Validated Design enforce per-account concurrent playback l operation",
        "description": "The gateway forwards a validated Design enforce per-account concurrent playback l operation after authentication, quota, and idempotency checks."
      },
      {
        "id": "service-source",
        "source": "service",
        "target": "source",
        "type": "database-connection",
        "label": "Design enforce per-account concurrent playback l state commit",
        "description": "Commits session intent and device policy before downstream asynchronous work."
      },
      {
        "id": "source-stream",
        "source": "source",
        "target": "stream",
        "type": "event-stream",
        "label": "Design enforce per-account concurrent playback l event",
        "description": "An outbox or change stream publishes committed Design enforce per-account concurrent playback l transitions with replayable ordering."
      },
      {
        "id": "stream-workers",
        "source": "stream",
        "target": "workers",
        "type": "message-queue",
        "label": "Design enforce per-account concurrent playback l background work",
        "description": "Workers handle heartbeat expiry, analytics, and entitlement refresh with bounded retries and idempotent updates."
      },
      {
        "id": "workers-read-model",
        "source": "workers",
        "target": "read-model",
        "type": "data-flow",
        "label": "Design enforce per-account concurrent playback l serving update",
        "description": "Workers update the Design enforce per-account concurrent playback l projection and expose a freshness watermark."
      },
      {
        "id": "read-service",
        "source": "read-model",
        "target": "service",
        "type": "database-connection",
        "label": "Design enforce per-account concurrent playback l projection read",
        "description": "The Design enforce per-account concurrent playback l service reads the latest acceptable projection or authoritative state."
      },
      {
        "id": "service-client",
        "source": "service",
        "target": "client",
        "type": "http",
        "label": "Design enforce per-account concurrent playback l result",
        "description": "Returns the latest acceptable result within playback authorization p95 <=100ms."
      },
      {
        "id": "stream-monitoring",
        "source": "stream",
        "target": "monitoring",
        "type": "data-flow",
        "label": "Design enforce per-account concurrent playback l pipeline health",
        "description": "Monitors Design enforce per-account concurrent playback l consumer lag, processing health, freshness, and projection correctness."
      }
    ]
  },
  "deepDives": [
    {
      "title": "Consistency and idempotency",
      "points": [
        "Keep the Enforce per-account concurrent playback limits commit authoritative; let derived projections converge with visible version and freshness.",
        "Use idempotency keys, conditional Enforce per-account concurrent playback limits writes, event IDs, and versions so retries and replay are safe.",
        "Return Enforce per-account concurrent playback limits version and observedAt so clients can reason about freshness."
      ]
    },
    {
      "title": "Partitioning and hot keys",
      "points": [
        "Partition Enforce per-account concurrent playback limits by its primary read and write keys, then isolate hot tenants, users, items, or regions.",
        "Bound Enforce per-account concurrent playback limits work with backpressure and queue isolation so one hot key cannot dominate the cluster.",
        "Keep a replay or reconciliation path for Enforce per-account concurrent playback limits projection drift and partial failures."
      ]
    },
    {
      "title": "Failure and recovery",
      "points": [
        "Serve the last acceptable Enforce per-account concurrent playback limits projection or an explicit degraded response when workers fail.",
        "Retain durable Enforce per-account concurrent playback limits records for replay and make each downstream effect independently retryable.",
        "Measure Enforce per-account concurrent playback limits latency, backlog, freshness, error rate, and correctness drift."
      ]
    }
  ],
  "tradeoffs": [
    {
      "title": "Strong consistency versus throughput",
      "recommendation": "Strongly protect the business commit and use versioned eventual projections for high-volume reads.",
      "caution": "Making every derived read synchronous increases coordination and tail latency."
    },
    {
      "title": "Push versus pull processing",
      "recommendation": "Push bounded events to workers and pull durable snapshots on reads or recovery.",
      "caution": "Unbounded fanout or full scans make bursts and hot keys expensive."
    },
    {
      "title": "Managed service versus custom control",
      "recommendation": "Start with managed primitives where their limits match the access pattern, then isolate replaceable interfaces.",
      "caution": "Provider defaults do not remove the need for quotas, replay, observability, and data lifecycle design."
    }
  ],
  "commonMistakes": [
    "Putting all work on the synchronous request path.",
    "Choosing a data store before writing the access patterns and consistency contract.",
    "Ignoring retries, duplicate events, late data, deletion, privacy, or authorization.",
    "Leaving hot partitions, backpressure, stale reads, and recovery unspecified.",
    "Allowing unbounded requests or exports to compete with normal traffic."
  ],
  "followUps": [
    {
      "question": "What happens when the primary worker is unavailable?",
      "answer": "Acknowledge only after the durable boundary, serve the last acceptable projection, queue retryable work, and replay or reconcile after recovery."
    },
    {
      "question": "How would you support a tenfold traffic spike?",
      "answer": "Pre-scale partitions, apply admission control and backpressure, isolate hot keys, cache immutable snapshots, and make freshness degradation visible."
    },
    {
      "question": "How do you verify correctness?",
      "answer": "Use idempotency metrics, version gaps, sampled source-to-projection comparisons, replay tests, and a shadow rebuild before repair."
    }
  ],
  "rubric": [
    {
      "criterion": "Requirements",
      "description": "The answer defines the start or renew a playback session workflow, scale, latency, durability, and failure contract."
    },
    {
      "criterion": "Data model",
      "description": "Entities and keys support enforce per-account concurrent playback limits across devices and regions, authorization, privacy, and lifecycle changes."
    },
    {
      "criterion": "Scalability",
      "description": "Partitioning, cache behavior, queues, backpressure, and hot-key handling are explicit."
    },
    {
      "criterion": "Reliability",
      "description": "Idempotency, replay, recovery, degraded reads, and observability protect committed state."
    },
    {
      "criterion": "Tradeoffs",
      "description": "The candidate can explain Enforce per-account concurrent playback limits consistency, push/pull, managed services, and the scale boundary."
    }
  ]
}
